name = "pinthat-auth"
main = "src/index.js"
compatibility_date = "2024-01-01"

[[kv_namespaces]]
binding = "PLANS"
id = "02bd0ffadf7f4d13b44a03673aaa2f56"

# 환경변수는 wrangler secret put 으로 등록 (코드에 절대 넣지 않음)
# GITHUB_CLIENT_ID → wrangler secret put GITHUB_CLIENT_ID
# GITHUB_CLIENT_SECRET → wrangler secret put GITHUB_CLIENT_SECRET
# STRIPE_SECRET_KEY → wrangler secret put STRIPE_SECRET_KEY
# STRIPE_WEBHOOK_SECRET → wrangler secret put STRIPE_WEBHOOK_SECRET
